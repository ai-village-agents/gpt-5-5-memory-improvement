# Identity memory wrapper

Pointer-only compatibility folder for shared L2 memory schemas.

Canonical GPT-5.5 identity/current-role facts remain in:

- `logs/current_state.md` for current Day 419 state.
- `README.md` and `INDEX.md` for repo purpose and bootloader policy.

Do not duplicate full platform instructions here; internal memory and current prompts remain authoritative for live constraints.

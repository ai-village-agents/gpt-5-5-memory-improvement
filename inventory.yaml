# Lightweight GPT-5.5 memory inventory for indexed/exchanged items.
# Native docs keep their own formats; this file only points at high-value items.
items:
  - id: active-memory-goal-day419
    created_day: 419
    updated_day: 419
    status: active
    kind: working
    summary: Improve GPT-5.5 memory by keeping internal memory as a bootloader and using this repo as the external memory OS.
    source: logs/current_state.md; docs/memory_operating_manual_v0.md
    path: logs/current_state.md
    retrieval_cue: At session start or when deciding what memory work to do next.
    internal_memory_policy: keep_summary
    expiry_or_review: Review at the next village goal change.
    last_verified: fa22204 Warn on stale pre-send passes

  - id: boot-memory-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Run scripts/boot_memory.py at session start to verify git sync, audit, smoke test, visible memory-health probes, and active-state files.
    source: scripts/boot_memory.py; SESSION_START.md
    path: scripts/boot_memory.py
    retrieval_cue: First action of each GPT-5.5 session.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review whenever session-start workflow changes.
    last_verified: 00cf720 Surface health probes during boot
    error_recovery: Use the fallback commands in SESSION_START.md if the wrapper fails.

  - id: pre-send-chat-guard
    created_day: 419
    updated_day: 419
    status: active
    kind: social
    summary: Use scripts/pre_send_chat.py before non-trivial chat; pass the exact draft and latest GPT-5.5 event so already-sent drafts are blocked and stale-PASS risk is visible.
    source: scripts/pre_send_chat.py; docs/pre_send_chat_checklist_v0.md; logs/current_state.md
    path: scripts/pre_send_chat.py
    retrieval_cue: Before send_message_to_chat unless the reply is clearly trivial.
    internal_memory_policy: keep_summary
    expiry_or_review: Review after any duplicate-chat or unsolicited-outreach mistake.
    last_verified: fa22204 Warn on stale pre-send passes
    error_recovery: If any event update arrives after helper PASS and contains GPT-5.5 AGENT_TALK, do not send in that same turn; restart pre-send.

  - id: consolidation-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Run scripts/prepare_consolidation.py before platform consolidate and make an explicit do-not-carry-forward decision.
    source: scripts/prepare_consolidation.py; docs/consolidation_checklist_v0.md
    path: scripts/prepare_consolidation.py
    retrieval_cue: Before any platform consolidate call.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review whenever consolidation scaffolding changes.
    last_verified: 628e878 Add inventory lookup helper


  - id: inventory-lookup-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Use scripts/inventory_lookup.py to find indexed memory items and print their canonical repo-relative paths.
    source: scripts/inventory_lookup.py; inventory.yaml; README.md
    path: scripts/inventory_lookup.py
    retrieval_cue: When a future session remembers a concept or item id but not which file to open.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review when inventory.yaml schema changes.
    last_verified: 628e878 Add inventory lookup helper
    error_recovery: Fall back to grep/search_memory.py over markdown files if inventory lookup fails.

  - id: compact-daily-log
    created_day: 419
    updated_day: 419
    status: active
    kind: episodic
    summary: daily_log.md is a one-line-per-checkpoint recovery log for recent memory-infra progress.
    source: daily_log.md; scripts/boot_memory.py
    path: daily_log.md
    retrieval_cue: At boot or when needing a quick recent-progress timeline without reading long logs.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review at each village goal change and trim stale lines.
    last_verified: current daily_log boot integration

  - id: reflection-synthesis-day419
    created_day: 419
    updated_day: 419
    status: active
    kind: reflection
    summary: Day 419 memory lessons compressed into promotion rules for what belongs in internal memory, scripts, inventory, or retirement.
    source: docs/reflection_synthesis_v0.md
    path: docs/reflection_synthesis_v0.md
    retrieval_cue: When deciding whether to promote, externalize, script, retire, or delete a memory lesson.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review at the next memory-goal retrospective or major consolidation change.
    last_verified: 3c84743 Record structural drift memory lesson

  - id: retired-youtube-goal-pointer
    created_day: 419
    updated_day: 419
    status: retired
    kind: pointer
    summary: The YouTube goal is complete; keep only a compact pointer and do not upload unpublished candidates without renewed gate review.
    source: logs/retired_goals_index.md; docs/youtube_memory_retirement_plan_v0.md
    path: logs/retired_goals_index.md
    retrieval_cue: Only if the YouTube goal is explicitly reopened or a user asks about prior channel state.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review only on explicit YouTube-goal reactivation.
    last_verified: final remembered YouTube commit 825035a

  - id: memory-metrics-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Use scripts/memory_metrics.py for a quick non-authoritative summary of compact-draft size, inventory distribution, guard presence, retrieval affordances, stale-review prompts, and action efficiency.
    source: scripts/memory_metrics.py; docs/peer_schema_comparison_v0.md
    path: scripts/memory_metrics.py
    retrieval_cue: When evaluating whether the external memory system remains compact, indexed, and executable without reading every doc.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review whenever success metrics or memory-system evaluation criteria change.
    last_verified: Day 419 stale-review/action-efficiency metrics enhancement
    error_recovery: Run scripts/audit_memory_repo.py and scripts/memory_smoke_test.py for authoritative pass/fail checks.

  - id: retrieval-self-test-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Use scripts/retrieval_self_test.py to test whether realistic memory questions retrieve expected answers through inventory, search, and canonical files.
    source: scripts/retrieval_self_test.py; Claude Opus 4.7 retrieval-test signal at HEAD 1359644
    path: scripts/retrieval_self_test.py
    retrieval_cue: When structural validation passes but I need to know whether future sessions can actually find the right memory.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review whenever inventory/search affordances or critical memory questions change.
    last_verified: Day 419 retrieval self-test addition
    error_recovery: If it fails, fix the consumer retrieval path or expected source of truth before trusting the memory repo.

  - id: goal-transition-procedure
    created_day: 419
    updated_day: 419
    status: active
    kind: procedural
    summary: Use scripts/prepare_goal_transition.py after a real admin goal announcement to list the active-state files and validation steps needed for a safe goal switch.
    source: scripts/prepare_goal_transition.py; docs/session_start_runbook_v0.md
    path: scripts/prepare_goal_transition.py
    retrieval_cue: When Shoshannah/admin announces a new village goal or the active goal appears stale.
    internal_memory_policy: keep_pointer
    expiry_or_review: Review whenever goal-transition workflow changes.
    last_verified: a906bf7 Cover goal transition rehearsal in smoke
    error_recovery: If goal state is confusing, search village history for the latest admin goal message before editing active-state files.
